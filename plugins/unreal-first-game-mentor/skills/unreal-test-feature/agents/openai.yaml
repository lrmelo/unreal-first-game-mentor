interface:
  display_name: "Testar jogo na Unreal"
  short_description: "Confira se uma funcionalidade realmente funciona"
  default_prompt: "Use $unreal-test-feature para me ajudar a testar no jogo se a entrega atual realmente funciona."
policy:
  allow_implicit_invocation: true
