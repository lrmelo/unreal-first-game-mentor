interface:
  display_name: "Corrigir problema na Unreal"
  short_description: "Investigue um erro da Unreal sem se perder"
  default_prompt: "Use $unreal-fix-problem para me ajudar a descobrir por que algo na Unreal não funcionou."
policy:
  allow_implicit_invocation: true
